namespace habitrack
{
class Tracker
{
public:
    Tracker();

    void loadImages(const std::vector<strin?>& imageFiles);
    void loadImages(const std::string& folder, bool recursive = false);

    getFeatures()

        void computeFeatures() private : Preferences mPrefs;
};
} // namespace habitrack
